model:
  n_feat: 128            # Number of features in U-Net
  epsilon: 0.0001          # Bound t for stability
  architecture: "noise"   # Architecture to use for the model
  channel_mults: [1,1,2,2,2]
  heads_at: [2,3,4]
  num_res: 2


data:
  angle_path: "/pdo/users/djtufto/cam2/angles/angles_january_cam_2data_dic.pkl"  # Path to angle data
  ccd_folder: "/pdo/users/djtufto/cam2/ccd"  # Folder containing processed images
  background_path: null  # Folder containing background images
  image_shape: [256,256]  # Shape of the images
  patch_size: null # Shape of the patches; null for no patches
  repeat_factor: 1 # Iterate repeat_factor times over the dataset each epoch
  camera_number: '2'
  mean: 0.251619186104019 #Dataset mean (for normalization)
  std: 2.118188109008096 #Dataset standard deviation (for normalization)

# Used for sampling from generate.py
generation:
  model_path: /pdo/users/djtufto/BEAM/model_outputs/cam2_11-116/checkpoint_epoch_149_20251204_235551.pth # Path to trained model checkpoint
  model_path_star: /pdo/users/djtufto/BEAM/model_outputs/conditional_noise/checkpoint_epoch_final_20251008_175925.pth # Path to trained model checkpoint
  params_file: null
  batch_size: 4 # Batch size
  n_sample: 1 # Number of samples per datapoint
  num_timesteps: 10 # Number of timesteps to log including endpoints
  image_shape: [256,256] # Shape of the images
  output_dir: "model_outputs/january_orbit_counts/" # Directory to save generated images
  orbit_range: null # Orbit range to sample from
  ema: True # Use EMA weights for sampling
  guidance_scale: 1.3 # Guidance scale for sampling